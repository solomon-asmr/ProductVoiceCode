# ProductVoiceCode

A React Native app to find product IDs using voice.

- Function: Tap mic -> Speak product name -> See product ID.
- Data: Uses a static, in-app JSON file.
- Tech: React Native, TypeScript, `react-native-voice`.
